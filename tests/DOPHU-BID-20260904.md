# Độ phủ mã nguồn ↔ kịch bản kiểm thử — Phân hệ Đấu thầu (BID)

| | |
|---|---|
| **Ngày lập** | 04/09/2026 |
| **Kịch bản** | `spec/tests/BID-01.md` … `BID-05.md` — 166 kịch bản |
| **Kết quả chạy** | `spec/tests/KQ-UAT-BID-20260903.md` — 118 đã chạy, 48 chưa |
| **Mã nguồn soi** | `abs-api` (9 route group, 3 controller, 3 service) · `abs-ms` (39 tệp, 45 server action) |
| **Test tự động sẵn có** | 38 hàm `Test*` trong 3 tệp Go; **0** test tự động ở `abs-ms` |

Báo cáo này trả lời ba câu khác nhau, đừng trộn vào nhau:

| Lớp | Câu hỏi | Bảng |
|---|---|---|
| **A · API** | Mỗi endpoint BID có kịch bản nào chạm tới, và đã chạy thật chưa? | §1 |
| **B · Test tự động** | Quy tắc nghiệp vụ nào đã có unit test Go, quy tắc nào chỉ dựa vào UAT thủ công? | §2 |
| **C · Giao diện** | Component nào của `abs-ms` đã được thao tác thật, component nào chưa ai chạm? | §3 |

---

## 1. Độ phủ API — `abs-api`

Ký hiệu: **✓** đã gọi thật trong đợt UAT · **~** gọi gián tiếp / một phần · **✗** chưa gọi lần nào.

### 1.1 Gói thầu — `/admin/tenders` (BID-01, BID-04)

| Method · Route | Handler | Kịch bản | Chạy | Ghi chú |
|---|---|---|:--:|---|
| `POST /` | `tenders.Create` | UAT-BID01-2, -11, -13 | ✓ | Sinh `KH_0004`, `KH_0005` |
| `GET /` | `tenders.List` | UAT-BID01-35 | ✓ | |
| `GET /:id` | `tenders.Get` | UAT-BID01-1, -3 | ✓ | |
| `PUT /:id` | `tenders.Update` | UAT-BID01-14, -17 | ✗ | **Không có nút sửa hồ sơ trên UI** — endpoint chưa lối vào |
| `GET /:id/boq/:versionId` | `tenders.GetBOQ` | UAT-BID01-8 | ✓ | |
| `PUT /:id/boq/:versionId` | `tenders.ReplaceBOQ` | UAT-BID01-24, -25, -36 | ✓ | Nơi phát hiện BUG-BID-005, -016 |
| `PUT /:id/boq/:versionId/columns` | `tenders.SetBOQColumns` | UAT-BID01-26…-29 | ✓ | Trùng nhãn cột (BUG-BID-022) |
| `GET /:id/boq/compare` | `reviews.Compare` | UAT-BID02-11, -27 | ✓ | Phân loại sai (BUG-BID-006) |
| `POST /:id/submit-review` | `tenders.SubmitForReview` | UAT-BID01-10, -37 | ✓ | Không chặn thiếu tệp (BUG-BID-015) |
| `GET /:id/assignments` | `tenders.ListAssignments` | UAT-BID01-1 | ~ | Chỉ đọc, panel không có nút sửa |
| `PUT /:id/assignments` | `tenders.ReplaceAssignments` | — | ✗ | **Không có kịch bản và không có lối vào UI** |
| `POST /:id/attachments` | `tenders.CreateAttachmentUpload` | UAT-BID01-9 | ✗ | Chưa tải tệp đính kèm gói thầu |
| `POST /:id/attachments/:fileId/confirm` | `tenders.ConfirmAttachment` | UAT-BID01-9 | ✗ | |
| `DELETE /:id/attachments/:fileId` | `tenders.DeleteAttachment` | — | ✗ | **Không có kịch bản** |
| `POST /:id/cancel` | `tenders.Cancel` | UAT-BID04-2, -3, -6, -7 | ✓ | |
| `POST /:id/reopen` | `tenders.Reopen` | UAT-BID04-10 | ✓ | Nhật ký không hiện trên UI |
| `POST /:id/clone` | `tenders.Clone` | UAT-BID04-9, UAT-BID01-33 | ✓ | Sinh `KH_0005` |

**9/17 endpoint đã gọi thật.** Ba endpoint đính kèm chưa chạy; `PUT /:id` và `PUT /:id/assignments` **không có lối vào trên giao diện** — đây là khoảng trống chức năng, không phải khoảng trống kịch bản.

### 1.2 Thẩm định — `/admin/tenders/:id/reviews` (BID-02)

| Method · Route | Handler | Kịch bản | Chạy | Ghi chú |
|---|---|---|:--:|---|
| `GET /:id/reviews` | `reviews.List` | UAT-BID02-1 | ✓ | |
| `GET /:id/reviews/:kind` | `reviews.Get` | UAT-BID02-2, -5 | ✓ | |
| `PUT /:id/reviews/technical` | `reviews.SaveTechnical` | UAT-BID02-3, -13, -16 | ✓ | |
| `PUT /:id/reviews/site` | `reviews.SaveSite` | UAT-BID02-9, -17 | ✓ | |
| `POST /:id/reviews/:kind/submit` | `reviews.Submit` | UAT-BID02-12, -18, -19 | ✓ | |
| `POST /:id/reviews/:kind/photos` | `reviews.CreatePhotoUpload` | UAT-BID02-8, -21 | ✓ | |
| `POST /…/photos/:photoId/confirm` | `reviews.ConfirmPhoto` | UAT-BID02-23 | ~ | Đường thành công chạy; đường "kho chưa có đối tượng" chưa dựng được |
| `DELETE /…/photos/:photoId` | `reviews.DeletePhoto` | UAT-BID02-24 | ✗ | Chỉ kiểm nút biến mất sau khi nộp |
| `POST /:id/reviews/merge` | `reviews.Merge` | UAT-BID02-27, -28 | ✓ | |

**7/9 endpoint đã gọi thật** — nhóm phủ tốt nhất của cả phân hệ.

### 1.3 Báo giá — `/admin/quotes` (BID-03)

| Method · Route | Handler | Kịch bản | Chạy | Ghi chú |
|---|---|---|:--:|---|
| `GET /tenders/:id/quote` | `quotes.Current` | UAT-BID03-1 | ✓ | |
| `GET /tenders/:id/quotes` | `quotes.History` | UAT-BID03-9 | ✓ | |
| `GET /tenders/:id/quote-exports` | `quotes.Exports` | UAT-BID03-40 | ~ | Đọc được, chưa đối chiếu nhật ký |
| `GET /quotes/:id` | `quotes.Get` | UAT-BID03-19 | ✓ | |
| `PUT /quotes/:id` | `quotes.Save` | UAT-BID03-3, -15, -16, -27 | ✓ | |
| `PUT /quotes/:id/cost-components` | `quotes.SetCostComponents` | UAT-BID03-20…-25 | ~ | Đọc + chặn tắt đã chạy; **thêm cấu phần riêng chưa chạy** |
| `POST /quotes/:id/request-approval` | `quotes.RequestApproval` | UAT-BID03-17 | ~ | Nút hiện đúng khi biên < ngưỡng, chưa bấm |
| `POST /quotes/:id/approve` | `quotes.Approve` | UAT-BID03-18 | ✗ | **Cần vai Ban lãnh đạo** |
| `POST /quotes/:id/send` | `quotes.Send` | UAT-BID03-19, -29 | ✓ | |
| `POST /quotes/:id/feedback` | `quotes.Feedback` | UAT-BID03-41, -43 | ✓ | |
| `POST /quotes/:id/export` | `quotes.Export` | UAT-BID03-34…-39 | ✓ | Tải cả PDF và Excel, mở kiểm nội dung |

**7/11 endpoint đã gọi thật.** `approve` là khoảng trống lớn nhất — nó là chốt chặn của BR-06 mà chưa lần nào chạy.

### 1.4 Hợp đồng & dự án — `/admin/contracts`, `/admin/projects` (BID-05)

| Method · Route | Handler | Kịch bản | Chạy | Ghi chú |
|---|---|---|:--:|---|
| `GET /tenders/:id/contract` | `contracts.GetForTender` | UAT-BID05-1 | ✓ | |
| `PUT /contracts/:id` | `contracts.Save` | UAT-BID05-6, -13, -21, -22 | ~ | Đọc form đã chạy; **chưa lưu hợp đồng** |
| `POST /contracts/:id/precheck` | `contracts.RunPrecheck` | UAT-BID05-10…-19 | ✓ | Chạy 2 lần, 9/9 hạng mục |
| `POST /contracts/:id/checks/:code/ack` | `contracts.AckCheck` | UAT-BID05-16, -17 | ✗ | Nút *Xác nhận bỏ qua* hiện đúng, chưa bấm |
| `POST /contracts/:id/sign` | `contracts.Sign` | UAT-BID05-20 | ✗ | **Cần 3 vai khác nhau** |
| `POST /contracts/:id/partner-signature` | `contracts.PartnerSignature` | UAT-BID05-24 | ✗ | |
| `POST /contracts/:id/remind` | `contracts.Remind` | UAT-BID05-9 | ✗ | |
| `POST /contracts/:id/init-project` | `contracts.InitProject` | UAT-BID05-25…-27, -32, -33 | ✗ | Nút khoá đúng khi chưa đủ chữ ký |
| `GET /projects/:id` | `contracts.GetProject` | UAT-BID05-30, -32 | ~ | Đọc qua `KH_0001` |
| `PUT /projects/:id/site-team` | `contracts.AssignSiteTeam` | UAT-BID05-31 | ✗ | **Cần vai Giám đốc dự án** |

**3/10 endpoint đã gọi thật** — nhóm phủ kém nhất, và lý do là quyền chứ không phải thiếu kịch bản. Bảy endpoint còn lại đều đã có kịch bản viết sẵn, chỉ chờ tài khoản đúng vai.

### 1.5 Tổng hợp lớp API

| Nhóm | Endpoint | Đã gọi | Tỷ lệ | Chặn bởi |
|---|---:|---:|---:|---|
| Gói thầu (BID-01, BID-04) | 17 | 9 | 53% | Thiếu lối vào UI (2), chưa tải tệp (3) |
| Thẩm định (BID-02) | 9 | 7 | 78% | — |
| Báo giá (BID-03) | 11 | 7 | 64% | Thiếu vai Ban lãnh đạo |
| Hợp đồng & dự án (BID-05) | 10 | 3 | 30% | Thiếu vai KT trưởng, Tổng GĐ, GĐ dự án |
| **Tổng** | **47** | **26** | **55%** | |

---

## 2. Độ phủ test tự động — 38 hàm Go, 0 test frontend

### 2.1 Quy tắc nghiệp vụ đã có unit test

| Test Go | Quy tắc phủ | UAT tương ứng | Chạy UAT khớp? |
|---|---|---|---|
| `TestMarginOf_MeasuredAgainstSellingPrice` | BID-03 BR-05 | UAT-BID03-3 | ✓ khớp |
| `TestQuoteLineRecalculate_PriceAndMarginAreInverses` | BR-05 hai chiều | UAT-BID03-3, -15 | ✓ khớp |
| `TestQuoteLineRecalculate_OverheadRaisesCostBeforeMargin` | BR-03 chi phí chung | UAT-BID03-20 | ~ chưa đổi % chung |
| `TestQuoteLineRecalculate_ManualPriceSurvivesBulkChange` | BR-05 khoá tay | UAT-BID03-16 | ✓ khớp (nhãn KHÓA TAY) |
| `TestQuoteCanSend_ApprovalIsBoundToTheApprovedNumber` | BR-06 ngưỡng biên | UAT-BID03-17, -18 | ~ **chưa chạy đường duyệt** |
| `TestMergeLines_ClassifiesEveryRowAndKeepsOriginals` | BID-02 BR-08 | UAT-BID02-27 | ✗ **test xanh nhưng thực tế sai** |
| `TestMergeLines_RenumbersDensely` | BR-08 | UAT-BID02-27 | ✓ |
| `TestValidateDistinctPartnerCodes` | BID-01 VAL-4 | UAT-BID01-25 | ✓ khớp nguyên văn |
| `TestNormalizeTolerance` | TS `tolerance` | UAT-BID03-32 | ✗ chưa chạy UAT |
| `TestRegionFromAddress` | BID-01 BR-06 | UAT-BID01-4 | ✗ **test xanh nhưng `Cam Ranh` sai** |
| `TestLocationPrefix` | BR-02 tiền tố | UAT-BID01-11, -13 | ✓ khớp |
| `TestContractPrecheckValid_StaleAfterAnEdit` | BID-05 BR-02 | UAT-BID05-21, -22 | ✗ chưa chạy UAT |
| `TestContractCheckBlocks_BlockingVersusWarning` | BR-01 mức chặn | UAT-BID05-4, -11…-18 | ✓ khớp |
| `TestContractSignChain_SequentialAndComplete` | BR-04 | UAT-BID05-20 | ✗ **test xanh nhưng thực tế 1 người ký 4 vai** |
| `TestValidatePaymentTerms_MustTotalOneHundred` | BR-08 | UAT-BID05-13 | ✓ khớp |
| `TestContractComputeValueAfterVAT` | TS `contract_value` | UAT-BID05-6 | ~ tính đúng, hiển thị thiếu |
| `TestTenderIsOverdue_OnlyBeforeTheQuoteGoesOut` | BID-01 BR-05 | UAT-BID01-22 | ✗ chưa chạy UAT |
| `TestNormalizeBOQColumns_RejectsWhatWouldCollide` | VAL-8 | UAT-BID01-28 | ~ chặn theo mã, **không chặn nhãn** |
| `TestNormalizeHiddenBOQColumns_RefusesTheLoadBearingOnes` | BR-09 | UAT-BID01-27 | ✓ khớp |
| `TestNormalizeExtra_KeepsOnlyDeclaredColumns` | TS `Extra` | UAT-BID01-35 | ~ |
| `TestDroppedColumnKeys` | BR-10 | UAT-BID01-30, -31 | ✗ chưa chạy UAT |
| `TestQuoteDocumentFileName` | XB tên tệp | UAT-BID03-34 | ✓ khớp |
| `TestRenderQuotePDF_ProducesADocument` | XB | UAT-BID03-34 | ~ chỉ kiểm "có tệp" |
| `TestRenderQuoteXLSX_FiguresAreNumbersAndTotalIsAFormula` | XB Excel | UAT-BID03-39 | ✓ khớp |
| `TestRenderQuoteXLSX_CarriesNoInternalFigures` | BR-10 | UAT-BID03-35 | ✓ khớp |
| `TestSelectQuoteColumns_DropsWhatTheTenderDoesNotUse` | BTH bộ cột | UAT-BID03-12 | ~ |
| `TestGuardUnpricedLines` | VAL-9 | UAT-BID03-37 | ✗ chưa chạy UAT |
| `TestRecalculate_DisabledComponentLeavesTheCost` | BR-03 | UAT-BID03-20 | ✗ chưa chạy UAT |
| `TestNormalizeCostComponents` / `TestDroppedComponentKeys_*` | BR-02, BR-03 | UAT-BID03-24, -25 | ✗ chưa chạy UAT |
| `TestTenderAccess_*` (7 hàm) | PL-B phân quyền | UAT-BID03-14, UAT-BID04-12 | ~ **xem §2.3** |

### 2.2 Ba test xanh nhưng hệ thống vẫn sai — khoảng trống nguy hiểm nhất

| Test | Vẫn xanh vì | Lỗi thật |
|---|---|---|
| `TestMergeLines_ClassifiesEveryRowAndKeepsOriginals` | Test gọi thẳng hàm `mergeLines` với đầu vào dựng sẵn | BUG-BID-006 — đường dữ liệu thật xếp dòng `0 → 1.260` vào nhóm "giữ nguyên" |
| `TestRegionFromAddress` | Test dùng địa chỉ có tên tỉnh đầy đủ | BUG-BID-007 — `Cam Ranh` (không kèm tỉnh) ra "Miền Nam" |
| `TestContractSignChain_SequentialAndComplete` | Test kiểm **thứ tự bước**, không kiểm **danh tính người ký** | BUG-BID-004 — một người ký cả 4 vai |

Đây là dạng khoảng trống tệ nhất: hàm thuần đúng, nhưng không có test nào chạy qua đường dữ liệu thật (repository → service → controller). **Bổ sung ngay ba test cấp service** dùng repo giả, không phải test hàm thuần.

### 2.3 Nghịch lý phân quyền

`tender_access_test.go` có 7 test và **tất cả đều xanh**, gồm `TestTenderAccess_BystanderWithPermissionStillCannotWork` — mô tả đúng BUG-BID-002. Nghĩa là **hành vi hiện tại là cố ý theo thiết kế**, và cái sai nằm ở chỗ khác: hệ thống không cho người dùng đường nào tự đưa mình vào bước, và luồng *Tiếp nhận gói thầu* không gán người tạo vào `intake` trong khi luồng *Clone* thì có. Cần một test khẳng định **hai luồng tạo gói phải cho ra cùng một tập quyền**.

### 2.4 Quy tắc hoàn toàn không có test tự động

| Quy tắc | Mức rủi ro | Ghi chú |
|---|---|---|
| BID-01 BR-03 — chặn chuyển thẩm định khi thiếu tệp BOQ | **Cao** | Chính là BUG-BID-015; không test nào chạm |
| BID-01 VAL-3 — khối lượng ≤ 0 | **Cao** | Chính là BUG-BID-016; bộ lọc nằm ở frontend nên Go test không thấy |
| BID-01 BR-01 — mã bất biến khi đổi địa chỉ | Trung bình | `PUT /:id` chưa có lối vào UI lẫn test |
| BID-02 BR-07 — tối thiểu 3 tệp, video tính như ảnh | Trung bình | Chỉ kiểm thủ công |
| BID-02 giới hạn tệp 12 MB / 200 MB / 40 tệp | Trung bình | Chỉ kiểm thủ công |
| BID-03 BR-11 — tệp vòng đã gửi không dựng lại | **Cao** | BUG-BID-019; không test nào chạm |
| BID-03 BR-04 — bộ cấu phần theo vòng | Trung bình | |
| BID-04 BR-03 — danh sách nơi nhận thông báo | Trung bình | BUG-BID-009 |
| BID-05 BR-06 — khởi tạo dự án trong một giao dịch | **Cao** | Không test nào chạm đường hoàn tác |
| BID-05 BR-10 — thông báo cá nhân hoá theo vai | Trung bình | |
| Trạng thái báo giá hợp lệ (CHECK constraint) | **Cao** | BUG-BID-020; migration không có constraint, không test nào chạm |

---

## 3. Độ phủ giao diện — `abs-ms`

**Không có một test tự động nào** ở `abs-ms` (không có test runner trong `package.json`). Toàn bộ độ phủ giao diện đến từ đợt UAT thủ công này.

| Component | Kịch bản | Thao tác thật | Ghi chú |
|---|---|:--:|---|
| `_components/tender-list.tsx` | UAT-BID01-35 | ✓ | Bộ lọc trạng thái/vùng miền/quá hạn **chưa bấm** |
| `_components/create-tender-dialog.tsx` | UAT-BID01-2…-5 | ✓ | |
| `_components/tender-status-badge.tsx` | — | ~ | In khoá thô khi status lạ (BUG-BID-020) |
| `_components/boq-table.tsx` | UAT-BID01-19 | ✓ | |
| `_components/boq-columns.ts` | UAT-BID01-27, -28 | ✓ | |
| `_components/sheet-view.tsx` | UAT-BID01-8 | ✗ | Nút *Xem toàn bộ* **chưa mở** |
| `_components/stone-fields.tsx` | UAT-BID03-32, -33 | ✗ | Sai số ±, danh mục bề mặt chưa kiểm |
| `[id]/_components/boq-editor.tsx` | UAT-BID01-8, -24, -25, -36 | ✓ | Nơi phát hiện BUG-BID-005, -016 |
| `[id]/_components/column-manager.tsx` | UAT-BID01-26…-31 | ✓ | |
| `[id]/_components/tender-actions.tsx` | UAT-BID01-10, UAT-BID04-2 | ✓ | |
| `[id]/_components/tender-attachments.tsx` | UAT-BID01-9 | ✗ | **Chưa tải tệp đính kèm gói thầu lần nào** |
| `[id]/_components/tender-crew.tsx` | UAT-BID01-1 | ~ | Chỉ đọc |
| `review/_components/technical-review-grid.tsx` | UAT-BID02-2…-6, -12…-16 | ✓ | |
| `review/_components/site-review-form.tsx` | UAT-BID02-9, -17 | ✓ | |
| `review/_components/site-photos.tsx` | UAT-BID02-8, -18…-24 | ✓ | Tải 3 ảnh + 2 tệp bị từ chối |
| `review/_components/boq-comparison.tsx` | UAT-BID02-11, -27 | ✓ | BUG-BID-006 |
| `review/_components/merge-panel.tsx` | UAT-BID02-10, -26, -28 | ✓ | BUG-BID-027 |
| `pricing/_components/pricing-grid.tsx` | UAT-BID03-2…-4, -15, -16 | ✓ | |
| `pricing/_components/cost-component-manager.tsx` | UAT-BID03-5, -6, -21 | ~ | **Chưa thêm cấu phần riêng** |
| `pricing/_components/commercial-terms.tsx` | UAT-BID03-47 | ✓ | Chảy đúng sang hợp đồng |
| `pricing/_components/round-decision.tsx` | UAT-BID03-7…-9, -41…-45 | ✓ | Chưa chạy *Đàm phán tiếp*, *Trượt thầu* |
| `pricing/_components/negotiation-timeline.tsx` | UAT-BID03-9 | ✓ | |
| `pricing/_components/proposal-sheet.tsx` | UAT-BID03-10…-12 | ✓ | BUG-BID-008 |
| `pricing/_components/export-quote-dialog.tsx` | UAT-BID03-13, -34…-39 | ✓ | BUG-BID-018, -019 |
| `contract/_components/precheck-panel.tsx` | UAT-BID05-2…-4, -10…-19 | ✓ | |
| `contract/_components/check-fix-targets.ts` | UAT-BID05-4 | ~ | Chưa bấm *Xác nhận bỏ qua* |
| `contract/_components/contract-form.tsx` | UAT-BID05-6, -7, -13 | ~ | **Chưa lưu hợp đồng** |
| `contract/_components/sign-chain.tsx` | UAT-BID05-5, -9, -20 | ~ | Chỉ đọc — chưa ký bước nào |
| `contract/_components/project-init-panel.tsx` | UAT-BID05-25…-33 | ✗ | Nút khoá đúng, chưa chạy |

**22/29 component đã thao tác thật (76%).** Bảy component chưa chạm: `sheet-view`, `stone-fields`, `tender-attachments`, `project-init-panel`, và ba component chỉ đọc.

**45 server action**: 24 đã gọi (53%). Chưa gọi: `updateTenderAction`, ba action đính kèm gói thầu, `approveQuoteAction`, `ackCheckAction`, `signContractAction`, `recordPartnerSignatureAction`, `remindSignerAction`, `initProjectAction`, `assignSiteTeamAction`, `fetchAssignableUsers` (gián tiếp).

---

## 4. Kết luận và thứ tự việc cần làm

### 4.1 Ba con số

| Lớp | Độ phủ |
|---|---|
| Kịch bản UAT đã chạy | **118 / 166 (71%)** |
| Endpoint API đã gọi thật | **26 / 47 (55%)** |
| Component giao diện đã thao tác | **22 / 29 (76%)** |
| Test tự động backend | 38 hàm, phủ ~18/47 quy tắc nghiệp vụ |
| Test tự động frontend | **0** |

### 4.2 Việc cần làm, theo thứ tự

1. **Bổ sung 3 test cấp service** cho ba chỗ "test xanh mà hệ thống sai" (§2.2) — dùng repository giả, chạy qua service thật, không test hàm thuần.
2. **Thêm CHECK constraint** cho `app_quote.status` và các cột trạng thái cùng loại; thêm test migration.
3. **Viết test cho 11 quy tắc chưa có test nào** (§2.4), ưu tiên 5 mục mức Cao.
4. **Cấp tài khoản 4 vai** (Ban lãnh đạo, Kế toán trưởng, Tổng GĐ, Giám đốc dự án) — mở khoá 21 endpoint và 48 kịch bản còn lại trong một đợt.
5. **Mở lối vào UI** cho `PUT /admin/tenders/:id` (sửa hồ sơ) và `PUT /:id/assignments` (gán nhân sự) — hai endpoint hiện không ai gọi tới được, và mục thứ hai chính là lối thoát cho BUG-BID-002.
6. **Cân nhắc test frontend**: `abs-ms` hiện không có test runner. Ba chỗ đáng test nhất là `boq-editor` (bộ lọc `qty > 0` đang gây mất dữ liệu), `boq-columns` (trùng nhãn) và `round-decision` (dải bước theo trạng thái vòng).
